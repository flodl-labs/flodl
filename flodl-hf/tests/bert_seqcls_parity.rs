//! PyTorch parity for `BertForSequenceClassification::from_pretrained`.
//!
//! Loads the fixture generated by `flodl-hf/scripts/parity_bert_seqcls.py`
//! (`fdl flodl-hf parity bert-seqcls`) and compares flodl's classifier
//! logits against the HF Python reference on the pinned prompt.
//!
//! `_live` — pulls the fine-tuned weights from the Hub. Run with
//! `fdl test-live`.

use std::path::Path;

use safetensors::{tensor::TensorView, SafeTensors};

use flodl::nn::Module;
use flodl::{Device, Tensor, Variable};
use flodl_hf::models::bert::{build_extended_attention_mask, BertForSequenceClassification};

const FIXTURE: &str = "tests/fixtures/bert_seqcls_parity.safetensors";
const MODEL_ID: &str = "nateraw/bert-base-uncased-emotion";

/// Same 1e-5 budget as the base-BERT parity test — classifier head is
/// one more linear projection on top of the pooler, so accumulated
/// numerical noise stays in the same ballpark.
const LOGITS_TOL: f32 = 1e-5;

fn parse_i64(v: &TensorView<'_>) -> Vec<i64> {
    v.data().chunks_exact(8).map(|c| i64::from_le_bytes(c.try_into().unwrap())).collect()
}
fn parse_f32(v: &TensorView<'_>) -> Vec<f32> {
    v.data().chunks_exact(4).map(|c| f32::from_le_bytes(c.try_into().unwrap())).collect()
}
fn shape_i64(v: &TensorView<'_>) -> Vec<i64> { v.shape().iter().map(|&d| d as i64).collect() }
fn max_abs_diff(a: &[f32], b: &[f32]) -> f32 {
    assert_eq!(a.len(), b.len(), "shape mismatch: {} vs {}", a.len(), b.len());
    a.iter().zip(b).map(|(x, y)| (x - y).abs()).fold(0.0_f32, f32::max)
}

#[test]
#[ignore = "network + ~440MB cache write"]
fn bert_seqcls_parity_vs_pytorch_live() {
    let dev = Device::CPU;

    let bytes = std::fs::read(Path::new(FIXTURE))
        .unwrap_or_else(|e| panic!("reading {FIXTURE}: {e} (run `fdl flodl-hf parity bert-seqcls` to regenerate)"));
    let st = SafeTensors::deserialize(&bytes).expect("parse parity fixture");

    let ids = st.tensor("inputs.input_ids").unwrap();
    let pos = st.tensor("inputs.position_ids").unwrap();
    let tt = st.tensor("inputs.token_type_ids").unwrap();
    let mk = st.tensor("inputs.attention_mask").unwrap();
    let ref_logits = st.tensor("outputs.logits").unwrap();

    let input_ids = Variable::new(
        Tensor::from_i64(&parse_i64(&ids), &shape_i64(&ids), dev).unwrap(), false,
    );
    let position_ids = Variable::new(
        Tensor::from_i64(&parse_i64(&pos), &shape_i64(&pos), dev).unwrap(), false,
    );
    let token_type_ids = Variable::new(
        Tensor::from_i64(&parse_i64(&tt), &shape_i64(&tt), dev).unwrap(), false,
    );
    let mask_flat_f32: Vec<f32> = parse_i64(&mk).iter().map(|&x| x as f32).collect();
    let mask_flat = Tensor::from_f32(&mask_flat_f32, &shape_i64(&mk), dev).unwrap();
    let attention_mask = Variable::new(build_extended_attention_mask(&mask_flat).unwrap(), false);

    let ref_data = parse_f32(&ref_logits);
    let ref_shape = shape_i64(&ref_logits);

    let head = BertForSequenceClassification::from_pretrained(MODEL_ID).unwrap();
    head.graph().eval();

    let out = head.graph()
        .forward_multi(&[input_ids, position_ids, token_type_ids, attention_mask])
        .unwrap();
    assert_eq!(out.shape(), ref_shape, "logits shape mismatch");

    let actual = out.data().to_f32_vec().unwrap();
    let diff = max_abs_diff(&actual, &ref_data);
    eprintln!("{MODEL_ID} logits max_abs_diff = {diff:.3e} (tol {LOGITS_TOL:.0e})");
    assert!(
        diff <= LOGITS_TOL,
        "seqcls parity: max_abs_diff = {diff:.3e} exceeds tol {LOGITS_TOL:.0e}",
    );
}
